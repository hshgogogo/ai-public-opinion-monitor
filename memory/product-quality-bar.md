# 产品质量线

产品应该像真实团队能持续使用的工作工具，而不是静态 demo 或只为展示而存在的页面。

每个有意义的工作流都必须让这些状态可见：

- 空状态
- 加载/进度状态
- 带有效摘要的成功状态
- 带 `error_type` 和可操作建议的失败状态
- 生成内容、推荐、分析或自动决策背后的证据

如果用户触发一个动作后只能看到沉默、模糊 spinner、或无法验证的生成文本，不允许标记为完成。
