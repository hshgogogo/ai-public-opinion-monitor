# 中文 Skill 骨架

将尖括号内容替换为具体行业、岗位或任务。保留主文件凝练，长内容拆到辅助目录。

```md
---
name: <kebab-case-name>
description: >
  <一句话说明这个 Skill 能力>。使用场景：用户要求<真实触发说法 1>、<真实触发说法 2>、<行业术语>、<口语表达>时使用；能处理<核心能力 1>、<核心能力 2>、<核心能力 3>。不适用：<相邻但不应触发的场景>。
argument-hint: "[输入对象] [输出形式]"
allowed-tools:
  - Read
  - Grep
  - Glob
---

# <Skill 中文名称>

## 定位

<说明这个 Skill 把哪类重复知识、流程或判断标准沉淀为可复用能力。>

## 工作流程

1. <理解输入和目标。>
2. <根据 Quick Reference 判断要加载哪些资源。>
3. <执行分析、生成、审查或转换步骤。>
4. <按模板输出。>
5. <做质量检查。>

## Quick Reference

| 用户意图/关键词 | 加载资源 | 用途 |
|---|---|---|
| <关键词组 1> | `reference/<file>.md` | <获取什么> |
| <关键词组 2> | `templates/<file>.md` | <生成什么格式> |
| <关键词组 3> | `scripts/<file>` | <执行什么确定性逻辑> |

## 资源加载契约

当用户需求涉及 <条件> 时，读取 `<路径>`，用于 <预期产出或判断>。

## 输出要求

- <格式要求>
- <必填字段>
- <质量标准>
- <遇到信息缺失时如何处理>
```

有副作用任务在 frontmatter 中额外加入：

```yaml
disable-model-invocation: true
allowed-tools:
  - Read
  - Bash(<固定安全脚本或命令前缀>:*)
```
